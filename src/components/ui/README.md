# UI Typography Guidelines

- Minimum readable size for **non-decorative text** is `text-xs` (12px).
- Use `text-sm` for helper copy under headings, denser surfaces, or any text that needs stronger readability.
- Preserve visual hierarchy with color, weight, and spacing (`text-muted-foreground`, `font-medium`, tracking), not by dropping below 12px.
